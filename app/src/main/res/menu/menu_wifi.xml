<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_sort"
        android:icon="@drawable/ic_settings"
        android:title="@string/sort"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/sort_by_ssid"
                android:title="@string/sort_by_ssid" />
            <item
                android:id="@+id/sort_by_security"
                android:title="@string/sort_by_security_protocol" />

            <item
                android:id="@+id/action_filter_submenu"
                android:title="@string/filter_by_security">

                 <menu>
                <!-- Dynamic filter options will be added here -->
                </menu>

            </item>

            <item
                android:id="@+id/action_default_view"
                android:title="@string/default_view"/>
        </menu>


    </item>
</menu>
